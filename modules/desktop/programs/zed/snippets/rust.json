{
  "element-iced": {
    "prefix": "$element-iced",
    "body": [
      "#[derive(Clone)]",
      "pub enum Message {}",
      "",
      "pub enum Event {}",
      "",
      "pub struct State {}",
      "",
      "impl State {",
        "pub fn new() -> Self {",
          "Self {}",
        "}",
      "",
        "pub fn update(&mut self, message: Message) -> Option<Event> {",
          "todo!()",
          "}",
      "",
        "pub fn view<'a>(&'a self) -> iced::Element<'a, Message> {",
          "todo!()",
        "}",
      "}"
    ]
  }
}
